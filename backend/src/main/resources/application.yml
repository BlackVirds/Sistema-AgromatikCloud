spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agromatik_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true #Despues de la actualizacion de InteliJ me pidió otro parametro para conectarse a la BD "allowPublicKeyRetrieval"
    username: root
    password: $@lano12
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

app:
  jwt:
    secret: "EstaEsMiClaveSecretaSuperLargaParaAgromatikCloud2025"
    expiration-ms: 86400000 # 24 horas en milisegundos
    api-key: "AgromatikSecureKeyForSensorsOnly"
